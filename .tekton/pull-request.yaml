apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: pr-pipeline
  annotations:
    pipelinesascode.tekton.dev/on-event: "[pull_request]"
    pipelinesascode.tekton.dev/on-target-branch: "[main]"
spec:
  pipelineSpec:
    tasks:
      - name: test
        taskSpec:
          steps:
            - name: fail-on-purpose
              image: alpine
              script: |
                #!/bin/sh
                echo "Starting test..."
                sleep 30  # Sleep for 30 seconds so you can see "in_progress"
                echo "This will fail!"
                exit 1
